import { part_1, part_2 } from './solution';

describe('Advent of Code {YYYY}, Day {DD}', () => {
  it('Part One', () => {
    expect(part_1()).toBe(null);
  });

  it('Part Two', () => {
    expect(part_2()).toBe(null);
  });
});